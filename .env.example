# Project: Hop-and-Barley
# Generated by codex-django

# Security
SECRET_KEY=replace-with-your-django-secret-key  # pragma: allowlist secret
DEBUG=True
# Required for encrypted model fields. Generate a Fernet key, for example:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=replace-with-your-fernet-field-encryption-key  # pragma: allowlist secret

# Application
ALLOWED_HOSTS=localhost,127.0.0.1
SITE_BASE_URL=http://127.0.0.1:8000/

# Database
# DATABASE_URL=postgres://user:pass@localhost:5432/Hop-and-Barley  # pragma: allowlist secret

# --- Database ---
POSTGRES_USER=django_user
POSTGRES_PASSWORD=secure_db_pass_2026_hb
POSTGRES_DB=hop_and_barley_db
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=secure_redis_pass_2026_hb
REDIS_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/0
